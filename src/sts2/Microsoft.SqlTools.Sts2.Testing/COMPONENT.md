# Microsoft.SqlTools.Sts2.Testing

**Role:** FakeDriver, scenario runner, simulator helpers, invariant checker.

**Allowed dependencies:** Runtime, Core, Contracts, Abstractions

**Forbidden:** product ship path (test-only), legacy namespaces

See docs/sts2/SPEC.md SS4 for the authoritative dependency matrix.

