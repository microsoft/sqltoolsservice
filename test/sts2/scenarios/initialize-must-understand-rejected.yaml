# STS2 scenario (SPEC $14.2). Editing expectations to match broken behavior is forbidden.
name: initialize-must-understand-rejected
tags: [initialize, contract, dialect:neutral]
status: active
milestone: M2
adapter: fake
steps:
  - request:
      method: v2/initialize
      params: { clientName: "scenario", mustUnderstand_quantumMode: true }
    expectError: { dataCode: Sts2.InvalidRequest, jsonRpcCode: -32600 }
expect:
  invariants: [I1, I5, I6, I7, I12]
