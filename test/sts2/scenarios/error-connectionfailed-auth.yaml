# STS2 scenario (SPEC $14.2). Editing expectations to match broken behavior is forbidden.
name: error-connectionfailed-auth
tags: [error, connection, dialect:neutral]
status: active
milestone: M2
adapter: fake
driver:
  open:
    - outcome: authFail
steps:
  - request:
      method: v2/connection.open
      params: { openId: open-1, profile: $profiles.fakeBasic }
    expectError: { dataCode: Sts2.ConnectionFailed.Auth, jsonRpcCode: -32040 }
expect:
  invariants: [I1, I12]
