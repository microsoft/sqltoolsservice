trigger: none
pr: none

stages:
- stage: Build
  jobs:
  - job: Build
    pool: 'ads-build-1es-hosted-pool'
    steps:
    - template: build.yml

- stage: Release
  variables:
  - name: skipComponentGovernanceDetection
    value: true
  dependsOn:
  - Build
  condition: and(succeeded(), eq(variables['RELEASE'], 'true'))
  pool: 'ads-build-1es-hosted-pool'
  jobs:
  - job: Release
    steps:
    - template: release.yml
